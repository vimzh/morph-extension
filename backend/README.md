# BASIC INSTRUCTIONS

- install UV with these instructions: https://docs.astral.sh/uv/getting-started/installation/

- set up virtual environment with `uv sync`

- run files with `uv run FILE_NAME.py`

- add dependencies with `uv add DEPENDENCY`